version: 2
jobs:
  build:
    docker:
    - image: circleci/node:8.11.1
    steps:
    - checkout
    - run:
        name: Code Linting
        command: |
          yarn && yarn lint 
    - run:
        name: Client and Server Testing
        command: |
          yarn test
  